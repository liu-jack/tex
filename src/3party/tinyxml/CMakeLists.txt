INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/tinyxml)

AUX_SOURCE_DIRECTORY(${CMAKE_CURRENT_SOURCE_DIR}/lib_tinyxml SOURCE_FILES)
ADD_LIBRARY(tinyxml STATIC ${SOURCE_FILES})

INSTALL(TARGETS tinyxml DESTINATION 3party-lib EXPORT mfw-libs)
INSTALL(DIRECTORY tinyxml DESTINATION 3party-include)
