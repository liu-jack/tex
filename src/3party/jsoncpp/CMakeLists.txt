INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

AUX_SOURCE_DIRECTORY(${CMAKE_CURRENT_SOURCE_DIR}/lib_json SOURCE_FILES)
ADD_LIBRARY(jsoncpp STATIC ${SOURCE_FILES})

INSTALL(TARGETS jsoncpp DESTINATION 3party-lib EXPORT mfw-libs)
INSTALL(DIRECTORY json DESTINATION 3party-include)
