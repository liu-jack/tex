INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)

ADD_EXECUTABLE(sdp2lua sdp2lua.cpp)
TARGET_LINK_LIBRARIES(sdp2lua parse)

INSTALL(TARGETS sdp2lua DESTINATION bin EXPORT mfw-libs)

ADD_SUBDIRECTORY(lua)
